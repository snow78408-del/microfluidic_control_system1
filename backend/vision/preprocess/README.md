# preprocess

`preprocess/` 用于放置图像识别前的视频预处理工具，当前包含 `rotate_video.py`。

本目录工具用于：
- 视频旋转
- 视频裁剪
- 分辨率调整
- 方向预处理
- 为后续检测提供标准化输入

职责边界：
- 本目录不直接承担液滴检测、跟踪和统计职责。

当前工具说明：
- `rotate_video.py`：支持 `90°逆时针`、`90°顺时针`、`180°`，并预留 `auto` 方向扩展位。
